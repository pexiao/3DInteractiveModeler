'use strict'

class Texture {
    // What should we store here? 
    // The picture or texture? 
    constructor(image) {
        this.image = image 
    }

    // Probably just store the picture and then load it into a texture later on
    // Need to load it later on though 
}